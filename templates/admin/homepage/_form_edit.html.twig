{{ form_start(form) }}

{{ form_errors(form) }}
<details>
    <summary>Section Accueil</summary>
    <div class="content">
        <div>
            {{ form_label(form.photoTopLeft) }}
            {% if form.vars.value.photoTopLeft is not null %}
            <img src="{{ asset(form.vars.value.photoTopLeft) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.photoTopLeft, {required: false, value: form.vars.value.photoTopLeft}) }}
        </div>

        <div>
            {{ form_label(form.photoMiddle) }}
            {% if form.vars.value.photoMiddle is not null %}
            <img src="{{ asset(form.vars.value.photoMiddle) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.photoMiddle, {required: false, value: form.vars.value.photoMiddle}) }}
        </div>

        <div>
            {{ form_label(form.photoBottom) }}
            {% if form.vars.value.photoBottom is not null %}
            <img src="{{ asset(form.vars.value.photoBottom) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.photoBottom, {required: false, value: form.vars.value.photoBottom}) }}
        </div>

        <div>
            {{ form_label(form.title) }}
            {{ form_widget(form.title) }}
        </div>

        <div>
            {{ form_label(form.subtitle) }}
            {{ form_widget(form.subtitle) }}
        </div>
    </div>
</details>


<details>
    <summary>Section À Propos</summary>
    <div class="content">

        <div>
            {{ form_label(form.pfp) }}
            {% if form.vars.value.pfp is not null %}
            <img src="{{ asset(form.vars.value.pfp) }}" alt="Preview" style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.pfp, {required: false, value: form.vars.value.pfp}) }}
        </div>

        <div>
            {{ form_label(form.descriptionTitle) }}
            {{ form_widget(form.descriptionTitle) }}
        </div>

        <div>
            {{ form_label(form.descriptionText) }}
            {{ form_widget(form.descriptionText) }}
        </div>
    </div>
</details>

<details>
    <summary>Section Contact</summary>
    <div class="content">
        <div>
            {{ form_label(form.contactDescription) }}
            {{ form_widget(form.contactDescription) }}
        </div>

        <div>
            {{ form_label(form.contactNumber) }}
            {{ form_widget(form.contactNumber) }}
        </div>

        <div>
            {{ form_label(form.contactLocation) }}
            {{ form_widget(form.contactLocation) }}
        </div>

        <div>
            {{ form_label(form.contactEmail) }}
            {{ form_widget(form.contactEmail) }}
        </div>

        <div>
            {{ form_label(form.contactPhotoTopRight) }}
            {% if form.vars.value.contactPhotoTopRight is not null %}
            <img src="{{ asset(form.vars.value.contactPhotoTopRight) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.contactPhotoTopRight, {required: false, value: form.vars.value.contactPhotoTopRight}) }}
        </div>

        <div>
            {{ form_label(form.contactPhotoBottomRight) }}
            {% if form.vars.value.contactPhotoBottomRight is not null %}
            <img src="{{ asset(form.vars.value.contactPhotoBottomRight) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.contactPhotoBottomRight, {required: false, value:
            form.vars.value.contactPhotoBottomRight}) }}
        </div>

        <div>
            {{ form_label(form.contactPhotoBottomLeft) }}
            {% if form.vars.value.contactPhotoBottomLeft is not null %}
            <img src="{{ asset(form.vars.value.contactPhotoBottomLeft) }}" alt="Preview"
                style="max-width: 200px; max-height: 200px;">
            {% endif %}
            {{ form_widget(form.contactPhotoBottomLeft, {required: false, value:
            form.vars.value.contactPhotoBottomLeft}) }}
        </div>
    </div>

</details>
<button class="btn create-button">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
<form method="post" action="{{ path('app_admin_footer_links_delete', {'id': footer_link.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ footer_link.id) }}">
    <button class="btn">Delete</button>
</form>

 {% set i = 0 %}
{% for photo in photos %}
{% set photos = photos|reverse %}
{% set i = i + 1 %}
<a class="grid-item magnific-img w-1/2 md:w-1/3 min-w-[45%] md:min-w-[20%] p-2" onclick="document.querySelector('html').classList.toggle('h-screen');"
    href="{{href}}">
    <img class="h-auto w-auto rounded-lg object-cover" width="200" height="100"
        src=src
        alt="{{ entityName ~ '-photo n°' ~ (i) }}">
</a>
{% endfor %}